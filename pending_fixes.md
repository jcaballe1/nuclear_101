| # | Category | Files | Issue / Improvement | Packaged Copilot Prompt | Model |
|---|----------|-------|---------------------|-------------------------|-------|
| 1 | Bug — physics correctness (Applied) | [src/components/NucleusVisualization.jsx](src/components/NucleusVisualization.jsx#L42-L80), [src/components/FissionAnimation.jsx](src/components/FissionAnimation.jsx#L5-L24) | Protons fill the inner layers first (`isProton = protonCount < config.protons`), so the nucleus shows red protons clustered at the centre and yellow neutrons only on the outside — physically misleading. The same bug exists in `FissionAnimation.generateNucleusParticles` (alternates by index → bands instead of mix). Also: U-235 should have a roughly even shell mix, not separated layers. | "In `NucleusVisualization.jsx` and `FissionAnimation.jsx`, replace the proton/neutron assignment in `generateParticles` and `generateNucleusParticles` with a deterministic shuffled interleave (use a seeded shuffle of an array containing `protons` red + `neutrons` yellow markers) so protons and neutrons are spatially **mixed** within the cluster. Keep the same total positions and layer math. Verify the fission stage 2 (U-236 vibrating) and the idle U-235 view both render mixed colours." | Opus |
| 2 | Bug — animation timing (Done) | [src/components/Scene2Fission.jsx](src/components/Scene2Fission.jsx#L11-L30) | `setTimeout(..., 3500)` reveals the balance scale, but the reset button has `transition={{ delay: 4 }}` measured from when the button mounts — produces a confusing 7.5s gap before the user can reset. Also, `FissionAnimation` runs ~3.6s of choreography but `onComplete` from `<FissionAnimation>` is a no-op; only `Scene2` time-keeps. | "In `Scene2Fission.jsx`, drive the balance reveal and reset-button visibility from `FissionAnimation`'s actual completion. Add an `onComplete` callback to `FissionAnimation.jsx` fired from a single trailing `setTimeout` matching the last animation's `delay + duration`. Remove the magic `3500` and the `delay: 4` on the reset button." | Sonnet |
| 3 | Bug — Scene 1 fission preview never renders fragments cleanly (Done) | [src/components/NucleusVisualization.jsx](src/components/NucleusVisualization.jsx#L262-L300) | The Scene-1 inline fission (`showFission && <Fragment 1 />…`) is short, unsynced with the modal alert in `TextPanel`, and leaves the SVG empty if the user dismisses the modal before fragments finish. | "In `NucleusVisualization.jsx`, refactor the inline `showFission` SVG block into a small `<MiniFissionBurst>` subcomponent with a self-contained 1.4s animation that always plays to completion regardless of the modal's open/closed state. Add a `key={showFission}` so it replays on each click of *Add Neutron*." | Sonnet |
| 4 | Bug — Particle pseudo-randomness (Done) | [src/components/Particle.jsx](src/components/Particle.jsx#L20-L50) | `Math.random()` is invoked inside `animate={{ x: [0, Math.random()*…, 0] }}` — evaluated once at render so every particle has a fixed jitter, not a Brownian wobble. | "In `Particle.jsx`, replace the `Math.random()` calls inside Framer Motion `animate`/`transition` props with stable per-instance values computed once with `useMemo`, and drive the wobble with `repeatType: 'mirror'` plus randomised but fixed delay/duration so the motion looks organic without re-randomising on every render." | Sonnet |
| 5 | Bug — Scene 6 progress labels desync (Done) | [src/components/NuclearWasteAnimation.jsx](src/components/NuclearWasteAnimation.jsx#L96-L110) | Step A is marked `done` the instant the rod leaves the reactor, including while it is still cooling in the pool; Step B is `active` before the rod is dropped in. | "In `NuclearWasteAnimation.jsx`, rewrite the `stA/stB/stC` derivation so: A=`active` while in pool and not cooled, `done` only after `canMoveToCask`; B=`active` once rod is in cask but `caskClosed`=false, `done` after sensor green; C unchanged. Add unit-style guard comments." | Sonnet |
| 6 | Physics correctness — Reactor loop (done) | [src/components/ReactorAnimation.jsx](src/components/ReactorAnimation.jsx#L210-L400), [src/components/Scene4TextPanel.jsx](src/components/Scene4TextPanel.jsx#L36-L42) | (Your note #1) The diagram pipes hot steam **directly** from the reactor to the turbine and the spent steam to a cooling tower with **no condenser, no heat exchanger, no feedwater pump**. For a PWR also no primary/secondary separation. Misleading for a "101". | "In `ReactorAnimation.jsx`, restructure the SVG into a proper PWR schematic: (a) primary loop = pressuriser + steam generator (heat exchanger) with a circulation pump; (b) secondary loop = steam generator → turbine → **condenser** (new component) → feedwater pump → back to steam generator; (c) tertiary loop = condenser ↔ cooling tower with a circulating water pump. Reuse `<FlowPipe>` and keep the existing colour language (orange=hot steam, blue=cool water, purple=spent steam). Update Scene4TextPanel ordered list to mention 'steam generator', 'condenser' and 'feedwater pump' explicitly. Add small labels to each new node." | Opus |
| 7 | Content gap — Radioactive decay & half-life (done) | [src/components/Scene5Radiation.jsx](src/components/Scene5Radiation.jsx), [src/components/Scene5TextPanel.jsx](src/components/Scene5TextPanel.jsx#L17-L60) | (Your note #2) The app introduces α/β/γ but never explains *why* nuclei decay, what spontaneous decay is, what a half-life is, or that the same decay laws govern medical isotopes and waste storage timescales. | "Extend Scene 5 with a new sub-section *'Why atoms decay'*. In `Scene5TextPanel.jsx` add a card that explains: unstable nuclei lose excess energy by emitting α/β/γ; decay is **spontaneous and probabilistic**; half-life = time for half of a sample to decay. In `Scene5Radiation.jsx` add a small `<HalfLifeWidget />` (new file `HalfLifeDecay.jsx` + css): 100 dots representing nuclei, each with a Poisson-style probability of decaying per tick; show a live counter `N(t) = N₀·(½)^(t/T½)` and a slider for half-life (1s–10s for visual purposes). Map example isotopes (I-131 = 8 days, Cs-137 = 30y, U-238 = 4.5 Gy) on a logarithmic timeline beneath the widget." | Opus |
| 8 | Content gap — Fission vs fusion, and energy density (done) | [src/components/LandingPage.jsx](src/components/LandingPage.jsx#L46-L62), [src/components/FinalReviewScene.jsx](src/components/FinalReviewScene.jsx#L130-L142) | A layperson will conflate fission with fusion. The energy-density comparison block in Scene 4 is commented out. There is no fossil-fuel/wind/solar comparison anywhere visible. | "Add one short paragraph + a 4-row comparison table to `LandingPage.jsx` (fission vs fusion in plain English) and uncomment / restyle the energy-density block in `Scene4TextPanel.jsx` as a clean grid: Coal, Natural Gas, Uranium-235, Hydrogen — MJ/kg and CO₂ g/kWh. In `FinalReviewScene.jsx`, replace the single 'fr-insight' paragraph with a 3-stat strip: world share, deaths/TWh (Our World in Data), gCO₂eq/kWh." | GPT |
| 9 | Content gap — Enrichment, moderator, coolant (done) | [src/components/TextPanel.jsx](src/components/TextPanel.jsx#L29-L40), [src/components/Scene3ChainReaction.jsx](src/components/Scene3ChainReaction.jsx) | Enrichment is mentioned in one paragraph with no visual; moderator and coolant are never named, yet they are essential to *why* chain reactions can be controlled. | "(a) In `TextPanel.jsx` add a small inline visualisation `<EnrichmentBar />` (or pure CSS bars): natural 0.7% U-235 → enriched 4% → weapons-grade 90%. (b) In `Scene3TextPanel.jsx` add a 'Moderator vs Coolant vs Control Rod' three-card explainer. In `ChainReactionAnimation.jsx`, optionally render light-blue 'water molecule' dots as a moderator field that slows fast neutrons (visually decreases neutron `vx` after passing one)." | Sonnet |
| 10 | Content gap — Reactor types & SMRs (done) | [src/components/Scene4Reactor.jsx](src/components/Scene4Reactor.jsx), [src/components/Scene4TextPanel.jsx](src/components/Scene4TextPanel.jsx) | The applet shows only one generic reactor. PWR/BWR/SMR/Gen-IV are absent — yet SMRs dominate today's policy debate (relevant to your Just Transition Fund context). | "Add a tab strip above the reactor SVG in `Scene4Reactor.jsx` with three options: *PWR*, *BWR*, *SMR*. Pass the selection into `ReactorAnimation.jsx` and conditionally render: PWR = full primary+secondary loop; BWR = single loop, steam directly from core; SMR = compact integral vessel containing steam generator inside, smaller footprint, modular icon. Update `Scene4TextPanel.jsx` with a one-paragraph blurb per type and a 'Why SMRs matter for Just Transition' callout." | Opus |
| 11 | UX — Skip vs Next gating (done) | [src/NucleusModule.jsx](src/NucleusModule.jsx#L210-L255) | If the user clicks *Skip*, the scene is never marked `done`, so the stepper dots stay grey forever and the *Final Review* shows fewer ✓ than the user actually saw. *Skip* and *Next* also occupy the same slot which is confusing. | "In `NucleusModule.jsx`, when the user presses *Skip*, mark the current scene as 'visited' (separate from 'done') and reflect it in the stepper as a faded dot. Always show *Next* as the primary CTA; demote *Skip* to a subtle text-link below. In `FinalReviewScene.jsx`, distinguish 'completed' (interacted) vs 'visited' (skipped) with two icon styles." | Sonnet |
| 12 | UX — Persist progress (done) | [src/NucleusModule.jsx](src/NucleusModule.jsx#L48-L60) | A page refresh wipes `done`, `scene`, and the landing flag. Embarrassing in an embedded teaching context. | "In `NucleusModule.jsx`, persist `{ scene, done: [...], landingSeen }` to `localStorage` under key `nuclear101-progress` on every change, and rehydrate on mount. Add a tiny *Reset progress* link in the footer that clears the key." | Sonnet |
| 13 | UX — Keyboard nav & a11y (done) | All scene components, [src/NucleusModule.jsx](src/NucleusModule.jsx) | No `←/→` keyboard navigation; SVG canvases have no `role="img"` / `aria-label`; many decorative emojis lack `aria-hidden`; colour-only state on Tempo gauge fails WCAG. | "Add a global `useEffect` in `NucleusModule.jsx` for `keydown` (←/→ = prev/next, 1-7 = jump to scene). Audit every SVG: add `role='img'` and a concise `<title>` describing the scene. Replace colour-only status indicators (TempGauge, sensor) with colour + text + icon. Run an axe-core audit and fix `aria-*` / contrast issues. Add `prefers-reduced-motion` media query to disable the heaviest Framer animations." | Gemini |
| 14 | UX — Mobile / responsive SVG (done) | [src/NucleusModule.css](src/NucleusModule.css), all `*Animation.css` | Scenes use fixed two/three-column grids; SVGs use `viewBox` but the container layouts break under ~900px. Stepper labels overflow on phones. | "Add responsive breakpoints (≤900px and ≤600px) across `NucleusModule.css`, `Scene[2-6]*.css`, `ChainReactionAnimation.css`, `ReactorAnimation.css`: stack text-panel and visualisation vertically; collapse the 7-step stepper into a numbered chip + dropdown; ensure SVGs resize via `width:100%; height:auto` while preserving `preserveAspectRatio`. Verify in DevTools at 375×812 and 768×1024." | Sonnet |
| 15 | UX — Replace linear-track radiation viz with penetration depth model (done) | [src/components/RadiationAnimation.jsx](src/components/RadiationAnimation.jsx) | The current 3 horizontal tracks tell the user "blocked / not blocked" but don't show the *physical* phenomenon: each particle has a stochastic stopping depth inside the material. A scatter / depth-profile is more truthful. | "Refactor `RadiationAnimation.jsx`: still show three streams, but instead of a hard wall, render the active shield as a wide rectangle and let each particle penetrate to a random depth sampled from a per-(particle, material) distribution: alpha — exponential mean ~5% of paper width; beta — gaussian centred mid-plastic; gamma — long exponential tail through lead with some leak-through. Plot heatmap dots where particles stop, building a real *attenuation profile* over time. Keep the drag-drop UX." | Opus |
| 16 | UX — Add a comprehension check / mini-quiz | New `src/components/Quiz.jsx`, [src/components/FinalReviewScene.jsx](src/components/FinalReviewScene.jsx) | Educational efficacy is much higher with active recall. Currently zero formative assessment. | "Create `Quiz.jsx` (+ css): 6 multiple-choice questions, one per scene (e.g. 'Which radiation type is stopped by paper?', 'Why does fission release energy?'). Show after the user clicks *Restart Journey* gate, or as a new optional 'Test Yourself' button on `FinalReviewScene.jsx`. Show per-question explanation on submit and a final score badge. Persist score in localStorage." | GPT |
| 17 | Polish — Glossary & tooltips (done) | All text panels | Terms like *isotope, MeV, atomic mass unit, Cherenkov, PUREX, baseload, MOX* appear without on-hover definitions. | "Create a small `<Term>` component that renders a dotted-underline span with a hover/focus tooltip. Build a `glossary.js` map of ~25 terms with one-sentence definitions. Wrap occurrences across all `Scene*TextPanel.jsx`, `BalanceScale.jsx`, `LandingPage.jsx`. Tooltip must be keyboard-accessible." | Sonnet |
| 18 | Polish — Audio/narration toggle (optional) | New `src/components/AudioToggle.jsx`, public/audio/ | A spoken narration of each scene's text would dramatically broaden accessibility for low-literacy / vision-impaired users and for outreach events. | "Add a global mute/unmute button in the header. For each scene, store an mp3 path keyed by scene id. On scene change, fade-out previous, fade-in current. Provide a written transcript already (the text panels), and add `<audio>` with `preload='none'`." | Grok |
| 19 | Polish — 3D nucleus (optional, last) | [src/components/NucleusVisualization.jsx](src/components/NucleusVisualization.jsx) | After fix #1, consider an optional toggle between the 2D SVG view and a true 3D `react-three-fiber` cluster — far better intuition for *why* a heavy nucleus is unstable (surface-to-volume ratio). | "Add a `<NucleusVisualization3D />` variant using `@react-three/fiber` and `@react-three/drei`: render protons (red) and neutrons (yellow) as instanced spheres on the surface of a sphere whose radius scales as A^(1/3). Add gentle vibration via per-frame noise. Provide a 2D/3D toggle in the visualisation header." | Opus |